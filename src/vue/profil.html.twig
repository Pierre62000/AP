{% extends "base.html.twig" %}
{% block title %} Profil - {{ parent() }} {% endblock %}

{% block contenu %}
<br>

<div class="container">
    <div class="card text-dark bg-white mt-5 border-primary">
        <div class="card-header">
            <h3>Informations d'utilisateur</h3>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-sm">
                    <p>Nom : {{form.nom}}</p>
                </div>
                <div class="col-sm">
                    <p>Prénom : {{form.prenom}}</p>
                </div>
            </div>
            <div class="row">
				<div class="col-sm">
                    <p>Email : {{form.email}}</p>
                </div>
                <div class="col-sm">
                    <p>Date d'embauche : {{form.dateEmbauche}}</p>
                </div>
            </div>
			<div class="row">
                <div class="col-sm">
                    <p> Profession : -</p>
                </div>
            </div>
			<button type="button" class="btn btn-primary">Modifier les informations</button>
		</div>
    </div>
</div>
{% endblock %}